# Spotify API credentials
# Get these from https://developer.spotify.com/dashboard
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id_here

# Optional: Override the redirect URI (auto-detected from current origin + base path)
# VITE_SPOTIFY_REDIRECT_URI=https://yourusername.github.io/repo-name/api/spotify/callback

# Note: SPOTIFY_CLIENT_SECRET is not needed - this app uses PKCE flow (client-side only)
